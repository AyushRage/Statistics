import numpy as data
from scipy import stats

size = int(input())
numbers = list(map(int, input().split()))
print(data.mean(numbers))
print(data.median(numbers))
print(int(stats.mode(numbers)[0]))